# FastAPI secret used for session signing
SECRET_KEY=changeme

# SQLite database path (relative to project root)
DATABASE_URL=sqlite:///./app/vape_crm.db

# Optional integrations
GOOGLE_MAPS_API_KEY=
DEFAULT_ADMIN_EMAIL=admin@example.com

# SMTP defaults (leave blank to log emails to console)
SMTP_HOST=
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=crm@example.com
